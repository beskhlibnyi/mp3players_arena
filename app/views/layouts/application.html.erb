<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Duelist" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    <%= stylesheet_link_tag "application", media: "all" %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
